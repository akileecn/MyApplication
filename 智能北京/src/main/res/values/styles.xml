<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 首页tab按钮 -->
    <style name="TabBtn">
        <item name="android:button">@null</item>
        <!-- 防按钮图标不显示但占位 -->
        <item name="android:background">@null</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:drawablePadding">5dp</item>
        <item name="android:textColor">@drawable/selector_tab_text_color</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- 视图分页指示器主题 -->
    <style name="MyTabIndicatorTheme" parent="AppTheme">
        <item name="vpiTabPageIndicatorStyle">@style/MyTabIndicatorStyle</item>
    </style>
    <style name="MyTabIndicatorStyle" parent="Widget.TabPageIndicator">
        <item name="android:textColor">@android:color/holo_red_dark</item>
    </style>

    <!-- 标题样式 -->
    <style name="TitleStyle">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="TitleButtonStyle">
        <item name="android:layout_margin">10dp</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:layout_width">30dp</item>
        <item name="android:layout_height">30dp</item>
    </style>
</resources>
